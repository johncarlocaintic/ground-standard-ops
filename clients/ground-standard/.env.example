# Ground Standard client .env — chained after root .env at runtime
# Usage: node --env-file=.env --env-file=clients/ground-standard/.env script.js
# Get real values from JC out-of-band. Never commit this file with values filled in.

# CloseBot — Ground Standard sub-account
CB_GS_API_KEY=

# GoHighLevel — GS Ads sub-account (Bobby's testing + production location)
GHL_GS_API_TOKEN=
GHL_GS_LOCATION_ID=isGl70YkeLEAiVckMhgT
